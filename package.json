{
  "name": "numpad-audio-player",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k \"npm:watch\" \"npm:typecheck\"",
    "watch": "esbuild src/main.ts --bundle --minify --format=esm --target=es2020 --outfile=dist/main.js --watch",
    "typecheck": "tsc --noEmit --watch",
    "build": "esbuild src/main.ts --bundle --minify --format=esm --target=es2020 --outfile=dist/main.js",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3",
    "esbuild": "^0.28.1",
    "fvtt-types": "github:League-of-Foundry-Developers/foundry-vtt-types#main",
    "prettier": "^3.9.5",
    "typescript": "^7.0.2"
  }
}